sync:
  defaults:
    flushOnCreate: true
    ignore:
      vcs: true
    permissions:
      defaultFileMode: 644
      defaultDirectoryMode: 755
  rtw-cli:
    alpha: './'
    beta: 'docker://docker@mw_cli_1/var/www'
    mode: 'two-way-resolved'
